export default class layoutDefault extends React.Component{
    render(){
        console.log(22);
        return (
            <p>
                Layout Default
            </p>
        );
    }
}

